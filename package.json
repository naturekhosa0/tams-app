{
  "name": "tams-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Traditional Authority Management System (TAMS) — staff accounts, the village register, resident accounts, land and permissions to occupy, the council record, notifications and the audit trail.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "test": "node --test tests/*.test.ts",
    "test:db": "bash supabase/tests/run-db-tests.sh",
    "test:all": "npm run typecheck && npm test && npm run test:db",
    "db:push": "supabase db push",
    "functions:deploy": "supabase functions deploy bootstrap-council-administrator create-staff-account manage-staff-account process-notification-emails emergency-admin-recovery",
    "import:legacy": "node scripts/import-legacy-data.mjs data/legacy-import",
    "import:dry-run": "node scripts/import-legacy-data.mjs data/legacy-import --dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "qrcode-generator": "^2.0.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "supabase": "^2.117.0",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
